{
  "name": "prime-client",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "test": "jest",
    "build:emu": "vite build --mode emu",
    "build:staging": " vite build --mode staging",
    "build:prod": "vite build --mode prod",
    "emu": "npm run build:emu && firebase emulators:start --only hosting",
    "deploy:staging": "npm run build:staging && firebase use staging && firebase deploy --only hosting",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
